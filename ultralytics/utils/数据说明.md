1.在plotting中修改 plot_images函数，使得可以显示图片的标签，且在Annotator中更改画图的通道数。
2.如果需要输出tn,fn,fp,tp，需要在metrics中的confusion_matrix函数中添加输出参数。
修改process_batch函数，更新验证时的混淆举证，输出错误判断的目标,并在val文件中调用它models->yolo->detect->val ，update_metrics
3.engine->validator中添加model.warmup(imgsz=(1 if pt else self.args.batch, 7, imgsz, imgsz))  # warmup
engine->predictor添加self.model.warmup(imgsz=(1 if self.model.pt or self.model.triton else self.dataset.bs, 7, *self.imgsz))

